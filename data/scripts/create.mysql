CREATE DATABASE IF NOT EXISTS pixyproxy;

CREATE USER IF NOT EXISTS 'pixyproxy'@'localhost' IDENTIFIED BY 'pixyproxy';

GRANT ALL PRIVILEGES ON codepromptu.* TO 'pixyproxy'@'localhost';

FLUSH PRIVILEGES;